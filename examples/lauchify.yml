
#
# Application configuration
# Configure: command options, app name and hooks
#
app: 
  name: "my-application"
  exec: "./run-app --opt=true"

  forever: true
  

updates:
  repository: "http://updates.example.com/app-updates"
  
  gpg:
    key: "D1AFCE1"


